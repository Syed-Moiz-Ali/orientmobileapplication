| ID | Severity | App | Module | File | Issue | Root Cause | Fix | Automated Test | Runtime Test | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FL-P0-001 | P0 | Shared | API Result | `packages/shared_core/lib/src/errors/result.dart` | No standard helper existed for Option B Result handling | Datasources manually switched on `Result<T>`, leading to mixed fake-default and discarded-result behavior | Added `Result<T>.unwrapOrThrow()` to return success data or throw the typed `AppException` | `flutter test test/result_test.dart` | N/A | VERIFIED |
| FL-P0-002 | P0 | Staff | Technician API | `apps/staff_app/lib/features/technician/data/datasources/technician_remote_datasource.dart` | Technician API failures were converted into empty/default successful models | Read methods used `failure: (_) => []` or empty response objects; write methods discarded returned `Result` | Migrated technician datasource reads and writes to `unwrapOrThrow()` so providers can distinguish success, thrown API failure, and offline queue fallback | `dart analyze lib/features/technician/data/datasources/technician_remote_datasource.dart`; `flutter analyze` for `packages/shared_core` | Not run | VERIFIED |
| FL-P0-003 | P0 | Staff | Technician offline fallback | `apps/staff_app/lib/features/technician/presentation/providers/technician_providers.dart` | Provider `catch` blocks could not run when remote failures were returned as `Failure` instead of thrown | Remote datasource swallowed failures before control returned to provider | Fixed upstream datasource so task action failures throw and existing provider offline queue path can execute | Covered by datasource analysis; dedicated provider queue unit test still needed | Not run | FIXED |
| FL-P0-004 | P0 | All Flutter apps | API failure state | `apps/*/lib`, `packages/shared_*` | Multiple datasources still convert API failures into empty data/default values | Full source inventory found broad `failure: (_) => []`, `failure: (_) => const ...`, and discarded write results beyond technician datasource | Not yet fixed outside the technician slice | TODO | TODO | TODO |
| FL-P0-005 | P0 | All Flutter apps | API contracts | Flutter datasources and backend controllers/DTOs | Full Flutter-backend contract audit remains incomplete | The current pass only inspected `ApiClient`, `Result`, technician datasource/provider, and relevant scan output | Continue endpoint-by-endpoint DTO checks before marking production-ready | TODO | TODO | TODO |
| FL-P1-001 | P1 | Staff | Static analysis | `apps/staff_app/lib` | App-wide analyzer has existing warnings unrelated to this slice | Advisor/supervisor UI files contain unused imports/locals and deprecated members | Not fixed in this slice | `flutter analyze` for `apps/staff_app` fails with 50 pre-existing lint issues | N/A | TODO |
| FL-P1-002 | P1 | All Flutter apps | Responsive/accessibility | `apps/*/lib` | Full viewport, text scaling, navigation, FAB overlap, and accessibility matrix remains incomplete | This pass focused on P0 API correctness for one high-risk datasource | Not fixed in this slice | TODO | TODO | TODO |
