# ===================================================================
# ORIENT-SYNC - offline sync / idempotency configuration
# Loaded by the gateway via spring.config.import (classpath:orient-sync.properties).
# ===================================================================

# H-6: how long idempotency keys/responses are kept before cleanup
# (see IdempotencyCleanupJob).
app.sync.idempotency-ttl-days=7
