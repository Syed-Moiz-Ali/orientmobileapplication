# ===================================================================
# ORIENT-MEDIA - media upload/storage configuration
# Loaded by the gateway via spring.config.import (classpath:orient-media.properties).
# ===================================================================

# Root folder for uploaded files (per-tenant subfolders are created below it).
# Override at deploy time: MEDIA_UPLOAD_PATH or app.media.upload-path.
# FIX (audit QA BUG-017): the hardcoded Linux path /data/orient/media broke every
# upload on non-Linux hosts, and a bare relative path (./media) resolves against
# Tomcat's context work directory (e.g. .../Tomcat/localhost/api#v1/media) and
# cannot be created. ${user.dir} is the JVM working directory (absolute).
app.media.upload-path=${MEDIA_UPLOAD_PATH:${user.dir}/media}
# URL returned to mobile clients. The files are served through the API context,
# not from the domain root. Override only when a CDN is introduced.
app.media.public-url-prefix=${MEDIA_PUBLIC_URL_PREFIX:${server.servlet.context-path:/api/v1}/media}
# CR-3: allowed content types - actual file content is magic-byte validated,
# the client Content-Type header is NOT trusted.
app.media.allowed-types=image/jpeg,image/png,image/webp,video/mp4,audio/m4a,audio/wav,image/gif
