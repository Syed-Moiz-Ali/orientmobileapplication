You are continuing the Orient Mobile Flutter product modernization.

The following Customer domains are now COMPLETE and FROZEN:

* Authentication
* Home
* Create Booking
* Booking Success
* Bookings
* Booking Details
* Service Status
* Contextual Approvals
* Vehicles
* Vehicle offline/sync identity
* Profile / Account
* Notifications inbox
* FCM notification lifecycle / push-open behavior

DO NOT redesign any of those domains.

==================================================
CURRENT TASK
============

Audit, redesign, implement and verify the COMPLETE CUSTOMER ROADSIDE ASSISTANCE / BREAKDOWN DOMAIN.

This is NOT:

"Make the breakdown form prettier."

This is:

"Determine what roadside assistance the product REALLY supports, then make the full request lifecycle truthful, useful and modern."

This includes all existing Breakdown-related:

* entry points
* request/create flow
* pending/requested state
* offline submission
* status/history if real
* any detail screen
* routing
* notifications integration
* dead code
* failure/retry behavior

==================================================
PRIMARY PRODUCT QUESTIONS
=========================

Before UI work determine:

1. What exactly does a customer submit?
2. What does the backend actually promise?
3. Does it dispatch assistance automatically?
4. Is there a workshop acceptance/assignment step?
5. Does the customer receive status updates?
6. Does a real Breakdown Detail endpoint exist?
7. Can a customer see previous breakdown requests?
8. Is live location actually collected?
9. Is the request offline-syncable?
10. What happens after a request succeeds?
11. Can it be cancelled?
12. Can it be retried safely?
13. Which notification types relate to breakdown?
14. Does the current UI claim things the backend does not support?

Do NOT begin visual redesign until these are answered.

==================================================
MULTI-AGENT EXECUTION — MANDATORY
=================================

Use at least SIX specialized subagents for initial discovery.

Run all independent audits IN PARALLEL.

The lead agent must:

* assign non-overlapping scopes
* collect evidence with file:line
* verify critical backend claims personally
* synthesize product decisions
* assign implementation ownership
* integrate serially where files overlap
* run final tests
* run visual QA
* stop after this domain

==================================================
SUBAGENT 1 — PRODUCT / UX AUDIT
===============================

READ ONLY initially.

Inspect every Customer Breakdown / Roadside screen and entry point.

Search for:

breakdown
roadside
SOS
emergency
dispatch
assistance
towing
location
GPS
vehicle breakdown

Inspect:

* Profile Roadside assistance entry
* any Home entry
* breakdown request page
* breakdown detail page
* success state
* request history
* notifications
* route definitions
* old widgets

Return:

CURRENT JOURNEY
ENTRY POINTS
SCREEN RESPONSIBILITIES
FAKE CLAIMS
DUPLICATED SCREENS
DEAD SCREENS
LOW-DENSITY AREAS
BROKEN ACTIONS
RECOMMENDED PRODUCT STRUCTURE
FILES

Do NOT modify code.

==================================================
SUBAGENT 2 — BACKEND CONTRACT AUDIT
===================================

READ ONLY.

Inspect backend:

CustomerBreakdownController
BreakdownService
Breakdown entity
BreakdownResponse
request DTO
assignment/update endpoints
advisor/staff breakdown flows
database schema
notification creation

Determine exact fields such as:

id
ref
vehicleId
vehicle
location
latitude
longitude
description
issue
status
priority
assigned staff
advisor
createdAt
updatedAt
notes
phone
branch

Do not assume these names.

For EACH actual field report:

SOURCE
REQUIRED?
EDITABLE?
CUSTOMER-FACING?
SAFE TO DISPLAY?
STATUS OWNERSHIP

Determine actual statuses and lifecycle.

Return:

REQUEST CONTRACT
RESPONSE CONTRACT
STATUS LIFECYCLE
CUSTOMER ENDPOINTS
STAFF/ADVISOR ENDPOINTS
ASSIGNMENT BEHAVIOR
CANCELLATION SUPPORT
HISTORY SUPPORT
BACKEND LIMITATIONS

==================================================
SUBAGENT 3 — LOCATION / GPS AUDIT
=================================

READ ONLY.

This is CRITICAL.

Inspect Flutter dependencies and production code for:

geolocator
location
permission_handler
maps
GPS
coordinates
address lookup
reverse geocoding
platform permissions

Determine:

* whether live location is actually collected
* whether location permission exists
* whether lat/lng is sent
* whether only a text address exists
* whether current copy falsely says auto-detected
* whether manual location entry is canonical
* whether map/location UI is real or decorative

Return:

REAL LOCATION CAPABILITY
FAKE LOCATION CLAIMS
PERMISSIONS
BACKEND LOCATION FIELDS
SAFE UX

Do NOT introduce geolocation merely because it sounds useful.

==================================================
SUBAGENT 4 — OFFLINE / SYNC AUDIT
=================================

READ ONLY.

Inspect:

breakdown offline create
SyncEngine
queue operation
idempotency key
local cache
failure queue
retry behavior
success reconciliation
temporary ids
notifications

Determine:

* whether a breakdown request can genuinely be queued offline
* what customer sees while queued
* whether success is falsely reported
* whether server response is reconciled
* whether duplicate requests are possible
* whether failed sync is recoverable
* whether app restart preserves pending requests

Return:

OFFLINE CONTRACT
TEMP ID BEHAVIOR
REPLAY
IDEMPOTENCY
RECONCILIATION
FAILURE STATE
CUSTOMER RECOVERY
RISKS

==================================================
SUBAGENT 5 — ROUTING / DETAIL / NOTIFICATION AUDIT
==================================================

READ ONLY.

Inspect:

breakdown routes
CustomerBreakdownDetailView
route extras
notification type `breakdownAssigned`
FCM routing
existing request success navigation

Determine:

* whether a real customer Breakdown Detail screen is justified
* whether it can load by id/ref
* whether it only accepts a full object via route extra
* whether its content is real or fabricated
* whether `breakdownAssigned` can route there safely
* whether a broad Breakdown page exists
* whether Notifications should continue falling back to inbox

Return:

ROUTES
DETAIL CAPABILITY
DEEP-LINK CAPABILITY
DEAD ROUTES
NOTIFICATION LIMITATIONS
PRODUCT RECOMMENDATION

==================================================
SUBAGENT 6 — TEST / VISUAL REGRESSION AUDIT
===========================================

READ ONLY.

Inspect existing Breakdown tests/goldens.

Identify coverage for:

request
offline
success
failure
validation
location
vehicle
status
history
detail
notification link
responsive
dark mode
text scale

Return:

EXISTING COVERAGE
STALE TESTS
MISSING TESTS
REQUIRED GOLDENS
HIGH-RISK REGRESSIONS

==================================================
SUBAGENT 7 — VISUAL PRODUCT QA
==============================

Run AFTER implementation.

Review every Breakdown golden.

Report per golden:

DEAD WHITESPACE
INFORMATION DENSITY
URGENCY CLARITY
FAKE CLAIMS
FORM CLARITY
PRIMARY ACTION CLARITY
MOBILE SPACE USE
DESKTOP SPACE USE
ERROR STATE QUALITY
DARK MODE
GENERIC AI LOOK

Any fake promise or giant empty emergency screen is BLOCKING.

==================================================
LEAD SYNTHESIS BEFORE IMPLEMENTATION
====================================

After all audits complete, the lead must explicitly decide:

1. What should this feature be called?
2. Is "Roadside assistance" the truthful primary name?
3. Is "Breakdown" an internal/backend term only?
4. Is this emergency dispatch or merely a workshop assistance request?
5. Is a detail/status screen justified?
6. Is history supported?
7. Is GPS supported?
8. Is vehicle selection required?
9. Is manual location required?
10. Is phone/contact required?
11. Is offline request safe?
12. What does success actually mean?
13. Which current claims must be deleted?

Only then implement.

==================================================
NAMING / PRODUCT LANGUAGE
=========================

Prefer truthful customer language.

Potential primary label:

Roadside assistance

Internal code may remain:

Breakdown

if changing domain names broadly would create unnecessary churn.

Do NOT use:

SOS
Emergency dispatch
Rescue unit
Tow truck dispatched

unless the backend/business genuinely guarantees those outcomes.

==================================================
KNOWN HISTORICAL FAKE CLAIMS
============================

Earlier audit found old copy such as:

"24/7 Roadside SOS"

"Avg response: 15–20 mins"

"Free towing up to 40km"

"Request Emergency Dispatch"

"Dispatching Unit..."

"Current GPS Location (Auto-detected)"

Profile cleanup already corrected some visible copy.

Search the ENTIRE Breakdown domain for remaining equivalents.

Remove anything unsupported.

==================================================
REQUEST FORM PRODUCT RESPONSIBILITY
===================================

The request form must answer:

Which vehicle needs help?

Where are you?

What happened?

How can the workshop understand the issue?

What am I actually submitting?

Do not make it feel like a flashy emergency landing page.

==================================================
VEHICLE SELECTION
=================

Use REAL customer vehicles.

If one vehicle:
preselect it.

If multiple:
allow compact selection.

If none:
determine whether backend permits request without vehicle.

If vehicle is required:
show a truthful Add vehicle path.

Do not fabricate a vehicle.

==================================================
VEHICLE IDENTITY
================

Use:

make/model
plate
relevant real metadata

No stock images.

No hardcoded car silhouettes.

Reuse established vehicle components where appropriate.

==================================================
OFFLINE / UNSYNCED VEHICLE
==========================

Respect the previously frozen vehicle identity system.

Do NOT send a temporary vehicle id to backend if the Breakdown API requires a persisted vehicle id.

If Breakdown request can safely queue offline with a temp id and reconciliation exists:
use the established mechanism.

Otherwise block honestly.

Do not create a second temp-id architecture.

==================================================
LOCATION
========

If real GPS is NOT supported:

use a clear manual location field.

Possible label:

Where are you?

Helper text:
Enter your current location or nearby landmark.

Do not say:

Auto-detected
Current GPS
Live location

without actual capability.

==================================================
LOCATION INPUT DENSITY
======================

Avoid a giant map placeholder if there is no map.

Use a normal purposeful text field.

If backend stores only free text:
do not fabricate coordinate/map precision.

==================================================
ISSUE / DESCRIPTION
===================

Inspect the actual DTO.

If one description field exists:

do not invent multiple fake categories unless they improve the payload honestly.

Potential UI:

What's wrong?

Describe what happened

Only add issue chips such as:

Flat tyre
Won't start
Overheating

if:

* backend genuinely supports free-text mapping cleanly
* product UX benefits
* chip simply fills the same real description field
* choices are not misleading

Do not create pseudo-diagnostics.

==================================================
CONTACT INFORMATION
===================

If backend already knows authenticated customer's phone/contact:

do not make the user re-enter it without reason.

If request DTO genuinely requires contact phone:
pre-fill from real session data if safe.

Do not fabricate missing phone.

==================================================
REQUEST REVIEW
==============

Determine whether a separate Review step is warranted.

For a simple form containing:

vehicle
location
description

a separate review screen may be unnecessary.

Do not create a wizard just because Booking uses one.

Prefer one efficient form if the product is simple.

==================================================
SUBMISSION CTA
==============

Use truthful copy.

Examples:

Request assistance

or:

Send assistance request

Do NOT use:

Dispatch now
Send rescue
Emergency dispatch

unless real.

==================================================
SUBMISSION LOADING
==================

Use:

Sending request…

Do NOT say:

Dispatching unit…

unless assignment has actually happened.

Prevent duplicate submits.

==================================================
SUBMISSION SUCCESS — CRITICAL
=============================

Success must communicate exactly what happened.

If backend only accepted the request:

say:

Request sent

not:

Help is on the way

unless assignment is already confirmed.

Possible truthful hierarchy:

Request sent

The workshop has received your roadside assistance request.

Reference:
BR-...

Vehicle
Location

We'll show updates here when they are available.

Only include claims supported by real status flow.

==================================================
SUCCESS ACTIONS
===============

Potential:

View request

ONLY if real Detail/status is supported.

Otherwise:

Back to Home

or:

Done

Do not create a dead View Request button.

==================================================
STATUS / DETAIL DECISION
========================

Create or retain Breakdown Detail ONLY if real backend/customer data makes it useful.

The detail page must add meaningful information such as:

request reference
vehicle
location
description
real status
assigned advisor/team if real
created time
updated state

Do NOT retain it if it only shows invented dispatch progress.

==================================================
IF DETAIL IS NOT JUSTIFIED
==========================

Delete the dead/unreachable detail implementation after verifying references.

Do not preserve it because a route once existed.

Notifications for `breakdownAssigned` should continue to fall back safely to inbox if there is no reliable destination.

==================================================
IF DETAIL IS JUSTIFIED
======================

Then List/request success vs Detail must have distinct purposes.

REQUEST FORM:
submit assistance request

DETAIL:
understand the current real request state

Do not duplicate.

==================================================
STATUS VOCABULARY
=================

Use backend statuses.

Do NOT invent:

Dispatching
Unit en route
Arriving soon

unless these are actual statuses.

Create one canonical customer-facing status mapping.

Reuse it everywhere.

==================================================
ASSIGNMENT
==========

If backend can assign staff/advisor:

show assignment only after a real assignment exists.

Potential:

Assigned to workshop team

or actual advisor/team name if returned.

Do not show:

Technician verified
Rescue driver
ETA

unless real.

==================================================
ETA
===

No estimated arrival time unless backend provides one.

Never calculate from request time.

Never reuse old "15–20 minutes" copy.

==================================================
TOWING
======

Do not mention towing unless:

* API/product explicitly supports it
* customer request includes it
* business contract guarantees something

Do not keep the old "free towing up to 40km" promise.

==================================================
HISTORY
=======

If customer API exposes multiple breakdown requests/history:

consider whether Roadside assistance page should show:

Current request
Recent requests

Only if genuine.

If only create exists:
do not invent history.

==================================================
ONE ACTIVE REQUEST
==================

Audit whether customer can create multiple simultaneous requests.

If backend permits it but product logically should not:
do NOT invent a client restriction without evidence.

If backend exposes an active request:
consider showing it prominently instead of immediately presenting another blank form.

==================================================
OFFLINE REQUEST — PRODUCT HONESTY
=================================

If queued offline:

do NOT say:

Request sent to workshop

Say something like:

Request saved on this device

It will be sent when you're back online.

Only if this accurately matches sync behavior.

==================================================
OFFLINE SAFETY
==============

Because Roadside Assistance may be urgent:

do not overstate offline reliability.

If offline request is merely queued locally, clearly indicate that the workshop has NOT received it yet.

This is more important here than in normal booking flows.

==================================================
FAILED SYNC
===========

If a queued roadside request exhausts retries:

do not silently leave it in a failed box.

Audit whether current sync architecture exposes failure state.

If it does:
show a compact recovery action.

Conceptually:

"We couldn't send this request."

Retry

If not:
report the limitation.

Because this is safety-adjacent, do not show false delivery.

==================================================
ONLINE FAILURE
==============

Keep all inputs.

Show clear error.

Allow Retry.

Do not clear the issue/location.

==================================================
TIMEOUT
=======

Audit whether request creation has an idempotency key.

If timeout makes server acceptance ambiguous:

do not blindly create a duplicate request.

Use existing idempotency architecture if present.

Report gaps.

==================================================
BREAKDOWN REFERENCE
===================

If server returns a public reference:

display it.

Prefer public ref over internal id.

Do not fabricate:

SOS-123
CASE-456

==================================================
NO STOCK / HERO IMAGERY
=======================

Do not add:

tow-truck image
warning illustration
map illustration
emergency red hero art

unless genuinely functional.

This product should feel calm and trustworthy.

==================================================
COLOR / URGENCY
===============

Roadside assistance can use stronger hierarchy than ordinary settings.

But do NOT make the entire page red.

Use error/emergency tones sparingly for:

important warnings
failed send
destructive/caution state

Primary request CTA can remain normal product primary color unless design system dictates otherwise.

==================================================
NO DEAD WHITESPACE
==================

Same product rule:

NO EMPTY-LOOKING SCREEN.

But do not fill the page with fake emergency messaging.

Use:

vehicle
location
issue
real request state
reference
real actions

to create density.

==================================================
MOBILE
======

Verify:

320
360
390
412
430

The form should feel efficient.

A user in a roadside situation should not scroll through decorative content.

Primary action should be easy to reach.

==================================================
STICKY ACTION
=============

A SafeArea bottom action may be appropriate:

Request assistance

if it does not cover content.

Keep form context visible.

==================================================
TABLET / DESKTOP
================

Do not render a narrow 390px emergency form in a 1440px viewport.

Potential wide composition:

LEFT
vehicle + location + issue

RIGHT
request summary / existing request state / submission context

ONLY using real content.

If form is too simple for two columns:
use a wider balanced single-column surface.

Do not invent a dashboard.

==================================================
SPARSE STATE
============

If only minimal fields exist:
use strong composition.

Do not create giant empty surfaces.

==================================================
ACCESSIBILITY
=============

Ensure:

* Roadside assistance heading
* selected vehicle semantics
* location label
* issue field
* error announcements
* offline warning
* failed send warning
* success announcement
* 48px targets
* 1.6x scale
* color not sole signal

==================================================
DARK MODE
=========

Verify:

fields
vehicle selector
warnings
success
failure
offline notice
status
dialogs

No hardcoded red/white assumptions.

==================================================
DEAD CODE
=========

Audit and potentially delete:

CustomerBreakdownDetailView

old fake dispatch widgets

old emergency hero widgets

unused status helpers

dead routes

Only after repo-wide grep.

==================================================
NOTIFICATION INTEGRATION
========================

The notification type:

breakdownAssigned

currently routes safely to Notifications inbox because no identifier exists.

Do NOT change that unless this task discovers:

* a real customer breakdown detail route
  AND
* a reliable identifier in FCM payload

Current payload reportedly has only type.

Therefore it will likely remain inbox fallback.

That is acceptable.

==================================================
TESTS — CONTRACT
================

Add tests for:

1. exact request payload
2. required fields
3. optional fields
4. real vehicle id
5. no temporary id sent online
6. manual location
7. issue description
8. no fake GPS claim
9. no fake ETA/towing promise
10. no duplicate submission

==================================================
TESTS — REQUEST
===============

Cover:

vehicle preselection
multiple vehicles
zero vehicles
location
description
validation
loading
success
server failure
timeout if representable
offline queue
failed sync if representable
retry
state preserved

==================================================
TESTS — SUCCESS
===============

Cover:

real request reference
real vehicle
real location
truthful success wording
no "help is on the way" unless backend state proves it

==================================================
TESTS — DETAIL/STATUS
=====================

If detail retained:

test every real backend status.

Test:

assignment absent
assignment present
unknown status
long content
stale/missing request
refresh

If detail removed:

test route cleanup and absence of dead navigation.

==================================================
TESTS — RESPONSIVE
==================

Cover:

320
360
390
430
1.6x
tablet
desktop
dark

==================================================
SOURCE GUARDS
=============

Search production Breakdown code for:

24/7
15–20
40km
Free towing
Auto-detected
GPS Location
Dispatching Unit
Emergency Dispatch
Help is on the way
Technician en route
Unit en route

Any remaining match must be supported by repository evidence and justified.

==================================================
GOLDENS
=======

Create/review only meaningful states.

Potential:

customer_breakdown_form_mobile
customer_breakdown_success_mobile
customer_breakdown_offline_mobile
customer_breakdown_error_mobile
customer_breakdown_tablet
customer_breakdown_desktop
customer_breakdown_dark_mobile

If detail is retained:

customer_breakdown_detail_mobile
customer_breakdown_detail_assigned_mobile

Do not create unnecessary goldens.

==================================================
VISUAL QA
=========

Independent Visual Product QA must review all changed goldens.

Specifically reject:

* giant emergency hero
* stock imagery
* excessive red
* empty white page
* misleading "dispatch" language
* fake location
* oversized cards
* poor form density
* weak offline warning
* success that implies server receipt when queued offline
* desktop phone-column island

==================================================
SCOPE BOUNDARY
==============

DO NOT redesign:

Home
Bookings
Booking Details
Status
Approvals
Vehicles
Profile
Notifications
Staff
Owner
CRM

Only minimal routing/provider integration is allowed.

==================================================
KNOWN PRE-EXISTING FAILURES
===========================

Do NOT fix unrelated:

shared_core `.env`
Staff JobDetailSheet
Owner KPI grid

==================================================
VERIFICATION
============

Run:

dart format

dart analyze customer_app

flutter test customer_app

Analyze shared packages only if modified.

Review all changed goldens manually.

==================================================
STOP CONDITION
==============

The Roadside Assistance domain is complete only when:

* claims match backend capability
* request payload is correct
* location UX is truthful
* offline state is honest
* duplicate submission is prevented
* success means exactly what happened
* dead detail/fake dispatch UI is resolved
* responsive layouts feel complete
* no dead whitespace
* tests pass

Then STOP.

DO NOT move to Staff automatically.

==================================================
FINAL REPORT FORMAT
===================

Start with:

PRODUCT STRUCTURE DECISION

Then report:

MULTI-AGENT EXECUTION

BACKEND BREAKDOWN CONTRACT

REAL STATUS LIFECYCLE

LOCATION CAPABILITY

OLD FAKE / UNSUPPORTED CLAIMS

FINAL REQUEST FLOW

VEHICLE BEHAVIOR

LOCATION / ISSUE INPUT

SUBMISSION

ONLINE / OFFLINE

SUCCESS STATE

DETAIL / STATUS DECISION

NOTIFICATION INTEGRATION

INFORMATION DENSITY

* mobile
* tablet
* desktop

DEAD CODE

TESTS / GOLDENS

VISUAL QA

ANALYZE / TEST RESULTS

REMAINING BACKEND / PRODUCT LIMITATIONS

STOP.
