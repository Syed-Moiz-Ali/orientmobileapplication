name: owner_app
description: Owner dashboard app for Orient Mobile
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^17.1.0
  flutter_riverpod: ^2.6.1
  google_fonts: ^8.1.0
  hive: ^2.2.3
  shared_core:
    path: ../../packages/shared_core
  shared_auth:
    path: ../../packages/shared_auth
  shared_models:
    path: ../../packages/shared_models

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

