import 'package:flutter/material.dart';

class AdvisorNotificationData {
  final String title, body, time;
  final IconData icon;
  final Color color;
  const AdvisorNotificationData(this.title, this.body, this.icon, this.color, this.time);
}

